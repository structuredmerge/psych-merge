items:
  - name: keep
    config:
      rules:
        - id: beta
          value: template_beta # beta inline
        # Removed rule docs
          # remove inline
        - id: alpha
          value: template_alpha # alpha inline
        - id: add
          value: template_add
