items:
  - name: keep
    config:
      # Keep docs
      keep: template_value # keep inline

      # Shared section docs
      # More shared docs

      # Remove docs
      # remove inline
      add: template_added
  - name: untouched
    config:
      stable: template_stable
