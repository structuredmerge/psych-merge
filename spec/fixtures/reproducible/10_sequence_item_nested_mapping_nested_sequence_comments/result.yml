items:
  - name: keep
    config:
      rules:
        # Keep docs
        - keep_rule # keep inline
        # Shared section docs
        # More shared docs

        # Remove docs
        # remove inline
        - add_rule
  - name: untouched
    config:
      stable: template_stable
